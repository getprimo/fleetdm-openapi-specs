# Fleet instance used by the validation harness (GET-only, read-only).
FLEET_URL=
# API token: Fleet UI > My Account > Get API token
FLEET_TOKEN=
# API version segment used by your instance (Fleet accepts "latest" or "v1").
FLEET_API_VERSION=latest
